/*
We have given A, B.
where, X+Y = A.
       X-Y = B. We have to find X,Y
*/

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    cout << (a + b) / 2 << " " << (a - b) / 2 << endl;
    return 0;
}