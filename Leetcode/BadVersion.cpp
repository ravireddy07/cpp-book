#include <iostream>
using namespace std;

class Solution
{
public:
    int firstBadVersion(int n)
    {
        int s = 1;
        int e = n;
        int ans -= 1;

        while (s <= e)
        {
            int mid = s + (e - s) / 2;
            if (isBadVers)
        }
    }
} int main()
{
}